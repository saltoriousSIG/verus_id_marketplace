import {
  NavbarContainer,
  SiteName,
  SiteTitle,
  SiteLogo,
  Details,
  CoffeeButton,
} from "./Nav.style";

export default function Nav() {

  return (
    <NavbarContainer>
      <SiteTitle>Verus IDS Listed Now</SiteTitle>
      <SiteTitle style='font-size: 16px'>Made with ❤️  by Saltorious@</SiteTitle>
    </NavbarContainer>
  );
}
